{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile --filter @workspace/api-server... --filter @workspace/exit360-web... && PORT=3000 pnpm --filter @workspace/exit360-web build && pnpm --filter @workspace/api-server build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @workspace/api-server start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
